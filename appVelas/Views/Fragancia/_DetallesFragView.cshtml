@model IEnumerable<appVelas.Models.Fragancia>
@{
    Layout = null;
}
    <input type="text" id="buscadorTabla" class="form-control mb-3" placeholder="Buscar..." autocomplete="off" style="width: 29%;" />
<table class="table">
    <thead>
        <tr>
            <th>
                Nombre
            </th>
            
            <th>
                @Html.DisplayNameFor(model => model.Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coste)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody id="tablaDatos">
        @foreach (var item in Model)
        {
            <tr data-coste="@item.Coste" id="eliminar-row-@item.IDFrag">

                <td class="col-buscar">
                    @Html.DisplayFor(modelItem => item.FragNombre)
                </td>               
                <td>
                    @Html.DisplayFor(modelItem => item.Cantidad)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Coste)
                </td>
                <td>
                    @Html.ActionLink("Actualizar", "ActualizarView", new { IDFrag = item.IDFrag, vista = "Frag" }, new { @class = "btn btn-outline-warning" }) |
                    @Html.ActionLink("Detalles", "DetallesView1", new { IDFrag = item.IDFrag, vista = "Frag" }, new { @class = "btn btn-outline-success" })
                    | <button class="btn btn-danger btn-eliminar btn-eliminar" data-tipo="Fragancia" data-id="@item.IDFrag">
    Eliminar
</button>
                </td>
            </tr>
        }
    </tbody>
</table>
